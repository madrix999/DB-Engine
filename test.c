int function(char *file) {
	int pos=0;
	int line=0;

	// run through first line, which will parse how many arguments+the names
	// run through the second line, which will parse the data types required for the arguments, which will be used to create head.
	// run through all the lines, char by char, set each line as a node, first one attached to head, rest attached to the last one initiated
}
