#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "dbFunctions.h"
#include "Functions.h"

int init() {
	puts("It Works");
	node_t * head = NULL;
	head = malloc(sizeof(node_t);
	if (head == NULL) {
		return 1;
	}
	
	head->val = 1
	head->next = malloc(sizeof(node_t));
	head->next->val = 2;
	head->next->next = null;
}

