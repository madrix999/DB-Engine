#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "dbFunctions.h"

int readFile(char *import[]) {	
}

void add_node(node_db * head, char *items[]) {
	node_db * current = head;
	while(current->next != NULL) {
		current - current->next;
	}

}
